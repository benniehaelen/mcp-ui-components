"""Code shared across the UI components.

The SQL controls (join diagram, query plan) share a small set of ``sqlglot`` AST
helpers and a colour palette (:mod:`.sql`), a seed SQL example (:mod:`.examples`),
and the standalone-HTML export used by the ``export_*`` tools (:mod:`.export`).
"""
